{
  "name": "extempored",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --workspace=@extempored/client",
    "build": "npm run build --workspace=@extempored/client",
    "start": "npm run start --workspace=@extempored/client",
    "test": "echo \"Test script needs to be configured in client/package.json\" && npm run test --workspace=@extempored/client --if-present",
    "lint": "npm run lint --workspace=@extempored/client"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2",
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
